package main

func main() {
	// How can init

	// var chSend = make(<-chan int)     // Sender // read-only channel
	// var chReceiver = make(chan<- int) // Receiver // write only channel
	// var chanDefault = make(chan int) // Default chan, that can make both // read-write
	// var DoneCh = make(chan os.Signal, 1) // Buffered channel

	// Examples how to use

	ExampleGoroutine2()

}
